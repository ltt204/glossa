services:
  glossa:
    container_name: glossa
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: glossa
    ports:
      - "5435:5432"
    volumes:
      - glossa_pg:/var/lib/postgresql/data

volumes:
  glossa_pg:
